# -----------------------------------------------------------------------------
# RL_Trading dependency baseline (Step 2 modernization pass)
#
# This project currently contains legacy pandas patterns (e.g. DataFrame.append)
# and legacy gym-based environments. The pins below target a stable bridge state
# for Python 3.10/3.11 while we migrate code in subsequent steps.
# -----------------------------------------------------------------------------

# data handling
numpy>=1.24,<2.0
pandas>=1.5,<2.0
PyYAML>=6,<7
stockstats>=0.6,<0.7
yfinance>=0.2.50,<0.3

# plotting + analytics
matplotlib>=3.7,<3.9
moviepy>=1.0,<3
pyfolio @ git+https://github.com/quantopian/pyfolio.git#egg=pyfolio-0.9.2

# model building
scikit-learn>=1.3,<1.6
gym==0.26.2
gymnasium>=0.29,<1.2
stable-baselines3[extra]>=2.2,<2.6
torch>=2.3,<2.6

# adapters and helpers
shimmy>=1.3,<2.0
bayesian-optimization>=1.4,<2.0

# testing
pytest>=7,<9

# packaging
setuptools>=68
wheel>=0.41
